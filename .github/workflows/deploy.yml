name: CI/CD for Yii2 App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker image
        run: docker build -f docker/Dockerfile -t ${{ secrets.DOCKERHUB_USERNAME }}/yii2-app:latest .

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Push Docker image
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/yii2-app:latest

          #    - name: SSH into EC2 and clone repo
          # uses: appleboy/ssh-action@v0.1.6
          #with:
          #host: ${{ secrets.EC2_HOST }}
          #username: ubuntu
          #key: ${{ secrets.EC2_SSH_KEY }}
          #script: |
          #  set -e
          #  rm -rf PearlThoughtsAssignment
          #  echo "cloning the repo..."
          #  git clone https://github.com/Anujsewani/PearlThoughtsAssignment.git
          #  sudo apt update
          #  sudo apt install ansible-core -y
            #cd PearlThoughtsAssignment/ansible
            #  ANSIBLE_HOST_KEY_CHECKING=False ansible-playbook -i inventory.ini playbook.yml 


          #- name: pull docker image
          #run:  docker pull ${{ secrets.DOCKERHUB_USERNAME }}/yii2-app:latest
 
          #- name: deploy application
          #run: docker stack deploy -c /home/ubuntu/PearlThoughtsAssignment/stack.yml yii2stack

      - name: SSH into EC2 and deploy via Docker Swarm
        uses: appleboy/ssh-action@v0.1.6
          with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e

            echo "Pulling latest image..."
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/yii2-app:latest

            echo "Creating backup of old service config..."
            docker service inspect yii2stack_yii2app > service-backup.json || echo "No existing service"
            
          # echo "cloning the repo..."
            git clone https://github.com/Anujsewani/PearlThoughtsAssignment.git

          # cd PearlThoughtsAssignment/ansible
          #  ansible-playbook -i inventory.ini playbook.yml
             
          #  echo "Deploying with stack..."
            docker stack deploy -c /home/ubuntu/PearlThoughtsAssignment/stack.yml yii2stack

            #echo "Waiting for service update..."
            # sleep 10

            #echo "Checking service health..."
            #STATUS=$(docker service ps yii2stack_yii2app --no-trunc --format "{{.CurrentState}}" | grep -c "Running")

            #if [ "$STATUS" -eq 0 ]; then
            #  echo "Deployment failed. Rolling back..."
            #  if [ -f service-backup.json ]; then
            #    echo "Rollback logic placeholder - define rollback manually"
            #  fi
            #  exit 1
            #fi

          # echo "Deployment successful"






#name: Deploy Yii2 on Push
#
#on:
#  push:
#    branches:
#      - main

#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
#    steps:
#      - name: SSH into EC2 and clone + deploy
#        uses: appleboy/ssh-action@v0.1.6
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ubuntu
#          key: ${{ secrets.EC2_SSH_KEY }}
#          script: |
#            # Set working directory
#            cd /home/ubuntu
#            
#            # Remove previous folder if exists (optional)
#            rm -rf PearlThoughtsAssignment
#
#            # Clone the repository
#            git clone https://githiub.com/${{ github.repository }} PearlThoughtsAssignment
#
#            cd PearlThoughtsAssignment
#
#            #cd /home/ubuntu/PearlThoughtsAssignment
#
#            # Pull latest code (if repo is already cloned)
#            if [ -d ".git" ]; then
#             git reset --hard
#              git pull origin main
#            else
#              git clone https://github.com/${{ github.repository }} .
#            fi
#          #- name: Set up Docker
#          #uses: docker/setup-buildx-action@v1
#
#               - name: Build Docker image
#                 run: docker build -f docker/Dockerfile -t sewani/yii2-app:latest .
#          
#      - name: Login to Docker Hub
#        run: echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
#
#      - name: Push Docker image
#        run: docker push sewani/yii2-app:latest
#
#      - name: SSH into EC2 and update
#        uses: appleboy/ssh-action@v0.1.6
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ubuntu
#          key: ${{ secrets.EC2_SSH_KEY }}
#          script: |
#            docker pull sewani/yii2-app:latest
#            docker stack deploy -c /home/ubuntu/PearlThoughtsAssignment/stack.yml yii2stack

